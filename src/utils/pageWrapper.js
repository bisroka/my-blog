import styled from "styled-components"

export const StyledPageWrapper = styled.main`
    display: flex;
    flex-direction: column;
    align-items: center; 
    overflow-x: hidden;
    min-height: 100vh;
`